---
name: example-skill
description: Example multi-platform skill for Codex CLI. Demonstrates the shared-core + thin-wrapper pattern.
---

# Example Skill — Codex wrapper

This is the **Codex CLI wrapper** for `example-skill`. The full skill definition lives in `shared/SKILL-CORE.md` in the same skill folder.

## When to use

Use when the user asks for:
- `example-skill`
- "run the example skill"
- "show me the example workflow"

## How to invoke

In Codex CLI:

```
example-skill
```

## Instructions

1. Read `shared/SKILL-CORE.md` for the platform-agnostic workflow and rules.
2. Execute the workflow using Codex CLI tools.
3. Keep responses concise and reference the shared core pattern.

## References

- `shared/SKILL-CORE.md` — full skill definition
